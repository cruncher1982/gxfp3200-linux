# Firmware

Do not commit proprietary firmware to this repository. If the sensor requires
an upload, document its provenance, license, file format, and integrity checks
before adding a `request_firmware()` implementation.
